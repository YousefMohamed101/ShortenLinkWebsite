import EChartsReact from "echarts-for-react";

function ShowLinkData({onClose}) {

    return(
        <>
            <div>
                <section>

                </section>

                <button onClick={onClose}>Back</button>
            </div>
        </>
    );

}

export default ShowLinkData;
